============
Installation
============

At the command line::

    $ easy_install django-user-verification

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-user-verification
    $ pip install django-user-verification
